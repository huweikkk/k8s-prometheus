apiVersion: v1
kind: Service
metadata:
  name: alertmanager
  namespace: newmonitoring
  labels:
    k8s-app: alertmanager
spec:
  type: NodePort
  ports:
  - name: http
    port: 9093
    targetPort: 9093
    nodePort: 30050
  selector:
    k8s-app: alertmanager
