apiVersion: v1
kind: Service
metadata:
  name: prometheus
  namespace: newmonitoring
spec:
  type: NodePort
  ports:
    - port: 9090
      targetPort: 9090
      nodePort: 30040
  selector:
    k8s-app: prometheus
