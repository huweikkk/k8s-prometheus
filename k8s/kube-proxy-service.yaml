apiVersion: v1
kind: Service
metadata:
  name: kube-proxy
  namespace: kube-system
  labels:
    k8s_app: kube-proxy
spec:
  ports:
  - port: 10249
    targetPort: 10249
    name: metrics
  selector:
    k8s-app: kube-proxy

