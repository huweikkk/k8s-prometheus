apiVersion: v1
kind: Service
metadata:
  name: kubelet
  namespace: kube-system
  labels:
    k8s-app: kubelet
spec:
  type: ClusterIP
  ports:
  - port: 10250
    name: https-metrics
    protocol: TCP
  selector:
    kubernetes.io/os: linux
